#include <stdio.h>
#include<math.h>
int main()
{
  int num1,num2,temp;
  scanf("%d %d",&num1,&num2);
  temp=num1%num2;
  while(temp!=0){
      num1=num2;
      num2=temp;
      temp=num1%num2;
  }
  printf("%d",num2);
  return 0;
}
